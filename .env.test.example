# Copy to .env.test (not committed) and adjust.
# Production: set ENABLE_TELEGRAM=true to load TelegramModule.
DATABASE_URL=postgresql://tmebel:tmebel@localhost:5433/tmebel_test
JWT_SECRET=e2e-jwt-secret-min-32-chars-for-hs256
JWT_ACCESS_TOKEN_TTL=15m
JWT_REFRESH_TOKEN_TTL=7d
COOKIE_DOMAIN=localhost
NODE_ENV=test
# ENABLE_TELEGRAM=false — omit or leave unset; TelegramModule loads only when ENABLE_TELEGRAM=true
